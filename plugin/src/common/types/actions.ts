export enum Actions {
  START,
  CANCEL,
  SELECT_AVAILABLE,
  SELECT_START,
  SELECT_CANCEL,
  SELECT_STOP,
  COPY,
  CUT,
  USE_AS_LASSO,
  PRETTIFY_LASSO,
  RESIZE_UI,
  NOTIFY,
  SET_API_KEY,
  GET_API_KEY,
  PASTE_API_KEY,
  GET_ACTIONS_LIMIT,
  PASTE_ACTIONS_LIMIT,
  END_ACTIONS_LIMIT,
}
