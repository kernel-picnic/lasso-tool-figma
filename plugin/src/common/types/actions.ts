export enum Actions {
  START,
  CANCEL,
  SELECT_AVAILABLE,
  SELECT_CHANGED,
  SELECT_START,
  SELECT_CANCEL,
  SELECT_STOP,
  COPY,
  CUT,
  USE_AS_LASSO,
  PRETTIFY_LASSO,
  ACTION_CANCEL,
  ACTION_FINISHED,
  RESIZE_UI,
  NOTIFY,
  GET_ACTIONS_LIMIT,
  PASTE_ACTIONS_LIMIT,
}
